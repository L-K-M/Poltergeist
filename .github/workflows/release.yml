name: Release

# Tag a version (scripts/release.sh X.Y.Z --push) to test, build, and publish
# Poltergeist's release artifacts: the app client for every platform folder
# the repo carries — Android APK, Linux packages (.deb + AppImage, x64 —
# Flutter publishes no linux-arm64 host artifacts), macOS/Windows desktop
# bundles, and an unsigned iOS IPA. Poltergeist has no server component
# (bookmark backup rides Séance's sync server), so unlike Séance there are no
# server binaries or Docker images here. The desktop bundles are unsigned
# (macOS: ad-hoc), so first launch needs the usual unidentified-developer
# step (macOS: right-click ▸ Open; Windows: SmartScreen ▸ Run anyway), and
# the iOS IPA must be re-signed to install (AltStore / Sideloadly / Xcode
# with a free Apple ID). Tagging before app/poltergeist_app exists fails the
# client jobs by design — there is nothing to release yet.
# No tag handy? The workflow_dispatch trigger below cuts the same release from
# a chosen commit and creates the tag for you (see its comment).
#
# Releases publish straight from CI with no human step (00 D23's
# 2026-09-03 decision change — Séance's posture): the release is created
# hidden while the client matrix attaches its assets, and the sums job
# makes it public only once SHA256SUMS, the notes, and the rehearsal-floor
# check are done — a public release is never partial or sum-less. The sums
# are an integrity channel (corruption, foreign mirrors), not an origin
# one. Runbook: docs/RELEASE.md.

on:
  push:
    tags: ['v*']        # e.g. v0.2.0
  # Cut a release without pushing a tag — for environments whose credential
  # can push branches but not tag refs (e.g. remote agent sessions). The tag
  # is created at the dispatched commit by the first gh-release step; the
  # input must match the pubspec `version:` (the scripts/release.sh lockstep
  # contract), since the APK/bundles bake that version in.
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag, e.g. v0.3.1 (created here if it doesn't exist)"
        required: true
        type: string

concurrency:
  # Keyed on the tag alone, not the full ref: a tag push and a manual
  # workflow_dispatch for the same tag must serialize behind one another,
  # because both create the same release and the release-existence guard
  # below is check-then-act, not atomic. Queuing, never cancel-in-progress —
  # cancelling a mid-flight publish would leave a half-attached draft.
  group: release-${{ inputs.tag || github.ref_name }}
  cancel-in-progress: false

permissions:
  contents: write       # create the GitHub Release

env:
  FLUTTER_VERSION: '3.47.2'

jobs:
  # Gate the release on the pure-Dart suite. A tag on a broken commit must
  # not publish a release. (The Flutter app's analyze+test and a per-platform
  # compile of every client run in CI on main/PRs, so the tagged commit is
  # already green there; the client jobs below still re-compile each
  # platform, which fails that platform's asset if it won't build.)
  test:
    name: Test (release gate)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      # Releases are created once and never updated: action-gh-release's
      # default when a release for the tag exists is to update it
      # (overwriting same-named assets), so fail first — for drafts as well
      # as published releases. Check-then-act is safe because the
      # workflow-level concurrency group serializes every run for the tag.
      # Recovery from a failed run: delete the release and the tag —
      # once a release exists the tag does too, and a surviving tag
      # pins the recovery run's release to the broken commit
      # (target_commitish is ignored when the tag already exists) —
      # then fix on main and dispatch on the fixed commit.
      - name: Refuse to overwrite an existing release
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          RELEASE_TAG: ${{ inputs.tag || github.ref_name }}
        run: |
          set -euo pipefail
          existing="$(
            gh api --paginate "repos/$REPO/releases" \
              --jq ".[] | select(.tag_name == \"$RELEASE_TAG\")
                        | .html_url + \" (draft=\" + (.draft|tostring) + \")\""
          )"
          if [[ -n "$existing" ]]; then
            {
              echo "a release for $RELEASE_TAG already exists:"
              echo "$existing"
              echo "delete it first, then re-run this workflow"
            } >&2
            exit 1
          fi
      - uses: dart-lang/setup-dart@v1
        with:
          sdk: stable
      - name: Resolve workspace
        run: dart pub get
      - name: Verify release tag and versions
        shell: bash
        env:
          RELEASE_TAG: ${{ inputs.tag || github.ref_name }}
        run: |
          dart run tool/release_version/bin/release_version.dart \
            check-tag --tag "$RELEASE_TAG"

          if git show-ref --verify --quiet "refs/tags/$RELEASE_TAG"; then
            tag_commit="$(
              git rev-parse --verify "refs/tags/${RELEASE_TAG}^{commit}"
            )" || {
              echo "release tag does not point at a commit" >&2
              exit 1
            }
            checkout_commit="$(git rev-parse HEAD)"
            if [[ "$tag_commit" != "$checkout_commit" ]]; then
              echo "release tag points at $tag_commit, not $checkout_commit" >&2
              exit 1
            fi
          else
            show_ref_status=$?
            if (( show_ref_status > 1 )); then
              echo "could not inspect release tag" >&2
              exit "$show_ref_status"
            fi
          fi

          order_arguments=(
            check-order
            --version "${RELEASE_TAG#v}"
          )
          tag_output="$(git tag --list 'v*')" || {
            echo "could not read release tag history" >&2
            exit 1
          }
          while IFS= read -r tag; do
            [[ -n "$tag" ]] || continue
            [[ "$tag" == "$RELEASE_TAG" ]] && continue
            order_arguments+=(--prior-tag "$tag")
          done <<< "$tag_output"

          dart run tool/release_version/bin/release_version.dart \
            "${order_arguments[@]}"
      - name: Resolve M0 benchmark harness
        if: hashFiles('tool/bench/pubspec.yaml') != ''
        working-directory: tool/bench
        run: dart pub get
      - name: Analyze
        run: dart analyze $(ls -d packages/*)
      - name: Analyze release gate
        run: dart analyze tool/license_gate
      - name: Analyze release version tool
        run: dart analyze tool/release_version
      - name: Analyze M0 benchmark harness
        working-directory: tool/bench
        run: dart analyze
      - name: Test
        run: |
          dirs=""
          for p in packages/*; do
            [ -d "$p/test" ] && dirs="$dirs $p"
          done
          if [ -z "$dirs" ]; then
            echo "no package has a test/ directory yet"; exit 1
          fi
          dart test $dirs
      - name: Test release gate
        run: dart test tool/license_gate/test
      - name: Test release version tool
        run: dart test tool/release_version/test
      - name: Test M0 benchmark harness
        working-directory: tool/bench
        run: dart test test

  # The app, once per client platform. `flutter build` is host-locked
  # (linux/windows only build on their own OS, macos/ios only on a Mac) —
  # hence a matrix over runners. fail-fast stays off so one platform's
  # breakage doesn't cancel the others: each job attaches its own asset to
  # the release independently. CI compiles the same matrix on main/PRs
  # (ci.yml `client` job — keep the two in step), so a tag should never be
  # the first time a client platform builds.
  client:
    name: Client (${{ matrix.target }})
    needs: test
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45   # first run downloads the Flutter toolchain
    strategy:
      fail-fast: false
      matrix:
        include:
          # The APK should sign with a committed, deliberately public,
          # debug-grade keystore (Séance's ci-release.jks pattern — personal
          # tool, no Play Store) so each release's APK upgrades the installed
          # app in place instead of demanding an uninstall. The app scaffold
          # milestone sets this up; no signing secrets are needed either way.
          - os: ubuntu-latest
            target: android
            build: apk --release
            files: poltergeist-android.apk
          # The .deb and AppImage ride along with the plain bundle (three
          # assets); the .deb keeps its versioned Debian filename, hence the
          # glob. x64 only: Flutter publishes no linux-arm64 host artifacts
          # (releases_linux.json is x64-only), so an arm runner can't build it.
          - os: ubuntu-latest
            target: linux
            build: linux --release
            flavor: x64
            files: |
              poltergeist-linux-x64.tar.gz
              poltergeist_*.deb
              poltergeist-linux-x64.AppImage
          - os: macos-latest
            target: macos
            build: macos --release
            files: poltergeist-macos-universal.zip
          # No Apple Developer account: built unsigned (--no-codesign) and
          # shipped as an IPA for users who re-sign and sideload. `build ipa`
          # stops at the unsigned .xcarchive — the Package step below zips
          # the IPA out of it explicitly.
          - os: macos-latest
            target: ios
            build: ipa --release --no-codesign
            files: poltergeist-ios-unsigned.ipa
          - os: windows-latest
            target: windows
            build: windows --release
            files: poltergeist-windows-x64.zip
    steps:
      - uses: actions/checkout@v4
      # Pin the JDK so the Gradle build is deterministic rather than tracking
      # the runner's default; keep in step with the app's compileOptions.
      - uses: actions/setup-java@v4
        if: matrix.target == 'android'
        with:
          distribution: temurin
          java-version: '17'
      # GTK toolchain for the Flutter Linux embedder, plus libsecret/jsoncpp
      # for flutter_secure_storage's keyring backend; imagemagick/file feed
      # scripts/package-linux.sh (icon resize, appimagetool).
      - name: Install Linux desktop toolchain
        if: startsWith(matrix.target, 'linux')
        run: >-
          sudo apt-get update && sudo apt-get install -y
          clang cmake ninja-build pkg-config libgtk-3-dev liblzma-dev
          libsecret-1-dev libjsoncpp-dev imagemagick file
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          cache: true
      # If the committed project signs Release with a developer identity the
      # runner doesn't have (the scripts/build.sh local path), switch the
      # build to ad-hoc signing — the signing class the app is designed to
      # support (entitlements and keychain use chosen to work ad-hoc-signed,
      # per the Séance lesson). Harmless when the project already specifies
      # ad-hoc signing (sed simply matches nothing).
      - name: Switch macOS signing to ad-hoc (runner has no certificate)
        if: matrix.target == 'macos'
        run: |
          sed -i '' \
            -e 's/"CODE_SIGN_IDENTITY\[sdk=macosx\*\]" = "Apple Development";/"CODE_SIGN_IDENTITY[sdk=macosx*]" = "-";/' \
            -e 's/CODE_SIGN_STYLE = Automatic;/CODE_SIGN_STYLE = Manual;/' \
            -e 's/DEVELOPMENT_TEAM = [^;]*;/DEVELOPMENT_TEAM = "";/' \
            app/poltergeist_app/macos/Runner.xcodeproj/project.pbxproj
      - name: Resolve workspace
        run: dart pub get
      - name: Resolve M0 benchmark harness
        working-directory: tool/bench
        run: dart pub get
      - name: Resolve app dependencies
        working-directory: app/poltergeist_app
        run: flutter pub get
      - name: Verify pinned Séance licenses
        run: dart run tool/license_gate/bin/check.dart # SEANCE_LICENSE_GATE_V1
      - name: Build
        working-directory: app/poltergeist_app
        run: flutter build ${{ matrix.build }} --no-pub
      # Prove Gradle consumed the synchronized pubspec code in the real APK.
      - name: Verify Android version code
        if: matrix.target == 'android'
        working-directory: app/poltergeist_app
        shell: bash
        run: bash "$GITHUB_WORKSPACE/scripts/verify-android-version.sh"
      # Stage each platform's product under its release asset name: product +
      # platform, no version (the release page already carries the version) —
      # except the .deb, whose filename is Debian's versioned convention.
      # Poltergeist is ASCII, so unlike Séance no post-build bundle rename is
      # needed anywhere.
      - name: Package
        shell: bash
        run: |
          case "${{ matrix.target }}" in
            android)
              cp app/poltergeist_app/build/app/outputs/flutter-apk/app-release.apk \
                 poltergeist-android.apk
              ;;
            linux)
              mv "$(ls -d app/poltergeist_app/build/linux/*/release/bundle)" \
                 "poltergeist-linux-${{ matrix.flavor }}"
              tar -czf "poltergeist-linux-${{ matrix.flavor }}.tar.gz" "poltergeist-linux-${{ matrix.flavor }}"
              # The mv above renamed the bundle out of Flutter's layout, so
              # point the packager at it explicitly (--bundle asks the ELF for
              # its arch).
              scripts/package-linux.sh --bundle "poltergeist-linux-${{ matrix.flavor }}" --output-dir .
              ;;
            macos)
              # ditto keeps the bundle's symlinks and metadata intact inside
              # the zip.
              cd app/poltergeist_app/build/macos/Build/Products/Release
              ditto -c -k --keepParent "$(ls -d *.app | head -1)" \
                "$GITHUB_WORKSPACE/poltergeist-macos-universal.zip"
              ;;
            ios)
              # An IPA is Payload/<app>.app zipped. `flutter build ipa
              # --no-codesign` stops at the unsigned .xcarchive and emits no
              # .ipa on its own, so zip the archive's app bundle explicitly
              # (D23's step) — unsigned; installing it means re-signing
              # (AltStore / Sideloadly / Xcode free account).
              app_dir="app/poltergeist_app/build/ios/archive/Runner.xcarchive/Products/Applications"
              app="$(ls -d "$app_dir"/*.app | head -1)"
              [[ -n "$app" ]] || { echo "no .app under $app_dir" >&2; exit 1; }
              mkdir Payload
              cp -R "$app" Payload/
              zip -qry poltergeist-ios-unsigned.ipa Payload
              ;;
            windows)
              mv "$(ls -d app/poltergeist_app/build/windows/*/runner/Release)" poltergeist-windows-x64
              7z a -bd poltergeist-windows-x64.zip poltergeist-windows-x64
              ;;
          esac
      - uses: softprops/action-gh-release@v2
        with:
          # On a tag push these two resolve to the pushed tag (and the
          # commitish is ignored); on workflow_dispatch they make the first
          # job to get here create the tag at the dispatched commit.
          tag_name: ${{ inputs.tag || github.ref_name }}
          target_commitish: ${{ github.sha }}
          # Hidden while the matrix legs attach; the sums job publishes
          # after the floor check, so a public release is never partial.
          # No human step — this is the whole D23 decision change.
          draft: true
          prerelease: >-
            ${{ startsWith(inputs.tag || github.ref_name, 'v0.') }}
          files: ${{ matrix.files }}

  # Every release carries SHA-256 checksums for all assets — SHA256SUMS
  # beside the assets, the same sums in the notes — computed by CI after
  # all assets attach (00 D23's decision change: an integrity channel,
  # not an origin one). Runs only when every client leg succeeded: sums
  # over a partial asset set would certify a broken rehearsal, and the
  # floor check below makes even a silent upload miss loud.
  sums:
    name: Checksums
    needs: client
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Compute SHA256SUMS over the release's assets
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          RELEASE_TAG: ${{ inputs.tag || github.ref_name }}
        run: |
          set -euo pipefail

          assets_dir="$(mktemp -d)"
          gh release download "$RELEASE_TAG" --repo "$REPO" \
            --dir "$assets_dir" --clobber

          # Rehearsal floor (07 §3.2): the APK and the Linux
          # .deb/AppImage/bundle must publish; everything they need builds on
          # ubuntu-latest, so a miss here is a pipeline bug the tag must wait
          # for.
          floor_missing=()
          for asset in 'poltergeist-android.apk' 'poltergeist_*.deb' \
                       'poltergeist-linux-x64.AppImage' \
                       'poltergeist-linux-x64.tar.gz'; do
            compgen -G "$assets_dir/$asset" >/dev/null \
              || floor_missing+=("$asset")
          done
          if ((${#floor_missing[@]} > 0)); then
            echo "floor asset(s) missing from the release: ${floor_missing[*]}" >&2
            exit 1
          fi

          ( cd "$assets_dir" && sha256sum -- * ) > SHA256SUMS

          # The notes own the labels D23 requires beside the artifacts — the
          # APK and IPA are rehearsal artifacts of the desktop codebase — and
          # the sums users verify downloads against (INSTALL.md points here).
          {
            cat <<'NOTES'
          **Android APK** — rehearsal artifact of the desktop codebase; Android
          is not a supported target of this release.
          **iOS IPA** — unsigned; re-sign to sideload. iOS is not a supported
          target of this release.

          `SHA256SUMS` ships beside the assets; the same sums follow below.

          ## SHA256 checksums

          NOTES
            cat SHA256SUMS
          } > RELEASE_NOTES.md

          gh release upload "$RELEASE_TAG" --repo "$REPO" SHA256SUMS --clobber
          gh release edit "$RELEASE_TAG" --repo "$REPO" \
            --notes-file RELEASE_NOTES.md
      # The publish itself: no human step, but only now that the floor
      # check passed and the sums and notes are attached. `gh release
      # ready` refuses a non-draft, so the created-once invariant holds;
      # flipping the flag touches no assets.
      - name: Publish
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          RELEASE_TAG: ${{ inputs.tag || github.ref_name }}
        run: gh release ready "$RELEASE_TAG" --repo "$REPO"
