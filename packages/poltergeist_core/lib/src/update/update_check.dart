/// D19/D23's link-only update check constants (07 §3.10): the checker
/// itself is Séance's [UpdateChecker] — ported behavior stays in
/// seance_core per D2 — pinned here to Poltergeist's own repository so
/// every call site reads the right repo without repeating the literal.
const poltergeistUpdateRepo = 'L-K-M/Poltergeist';
